name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3', '8.4']
        laravel: ['11.*', '12.*']
        exclude:
          - php: '8.2'
            laravel: '12.*'

    name: PHP ${{ matrix.php }} / Laravel ${{ matrix.laravel }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: dom, curl, libxml, mbstring, zip, pcntl, sqlite3
          tools: composer:v2
          coverage: none

      - name: Install dependencies
        run: |
          composer require "illuminate/support:${{ matrix.laravel }}" --no-interaction --no-update
          composer install --no-interaction --prefer-dist --no-progress

      - name: Run Pest tests
        run: vendor/bin/pest --colors=always

      # Blocking (W14/WS-G): findings were fixed (typed component render(),
      # pint auto-fixes) or scoped (optional Filament excluded from analysis).
      - name: Run PHPStan
        run: vendor/bin/phpstan analyse --no-progress

      - name: Run Pint (style check)
        run: vendor/bin/pint --test
